# Packages

Shared libraries consumed by apps.

| Package | Purpose |
|---------|---------|
| `shared` | Constants, utils, validators |
| `ui` | React components, layouts, themes |
| `types` | TypeScript types and event payloads |
| `config` | ESLint, TypeScript, Prettier presets |
