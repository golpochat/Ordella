{
  "name": "ordella",
  "version": "0.0.0",
  "private": true,
  "description": "Ordella monorepo — multi-tenant restaurant ordering platform",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {},
  "engines": {
    "node": ">=20"
  }
}
