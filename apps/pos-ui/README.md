# @ordella/pos-ui

Next.js 14 App Router POS interface.

## Run

```bash
npm run dev --workspace=@ordella/pos-ui
```

App runs on `http://localhost:3002`.

## Environment

Create `apps/pos-ui/.env.local`:

```bash
NEXT_PUBLIC_API_URL=http://localhost:3000/api/v1
```
