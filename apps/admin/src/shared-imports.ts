/** Verifies workspace path aliases resolve for admin. */
import { Button, Card } from '@shared-ui';
import { createApiClient, productSchema } from '@shared-utils';

export function adminSharedSmoke(): void {
  void Button;
  void Card;
  void createApiClient;
  void productSchema;
}
